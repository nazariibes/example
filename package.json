{
  "name": "example-target-repo",
  "version": "1.0.0",
  "description": "This repository is used as an example for testing out the script for updating deps",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "uuid": "^1.4.2"
  }
}
